バグ1
GUIを読みこむ時にモデルオブジェクトの位置がおかしかったり、無限に読み込んだりする

もしかして配置時の見本のを間違えて読み込んでる？

見本のを読み込んでた。
モデルオブジェクトをCreateするときに番号なし(見本のやつ)の名前が指定されていて、
見本と同じ座標になっていた

Create関数にGetNameで名前を取得するようにした方が良いかも
それか、自動で番号付ける

点けた


バグ2
GUIの読み込みで無限ループ
lavelに無効な文字列があるとダメ？

配置したオブジェクトのラベルに無効な文字列があるとズレる？

プレイヤーで無限ロードしてバグる

読み込み時に書き出し用にデータを保存する場面でparamに+= -1 してたのがいけなかった
そもそも読み込んだやつの保存はそれをそのまま書き出さないからここ間違ってても意味ない


更にラベルの終端文字を追加してなかった
これから直す

ステージ、プレイヤーのジョンで置いたらバグった

もしかして過剰にループしてる？
一旦乱数なしにしたら直るかも

乱数の処理消したらバグらなくなったからそこで確定かも

あとまだモデルオブジェクトがズレてる
これはゲーム側が悪い？
エンジンが悪い。多分GUIの反映させるところあたり

ゲーム側が悪かった
エディットデータを読み込む前に座標セットしたせい